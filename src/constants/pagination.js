// Shared pagination constants used across all pages that load posts/lists

export const POST_PAGE_SIZE = 4;       // posts per page/cursor-fetch
export const COMMENT_PAGE_SIZE = 10;   // comments per fetch
export const SCROLL_THRESHOLD = 200;   // px from bottom to trigger next load
